{
  "name": "connect-ia",
  "description": "Connect IA - Sistema de CRM e Automação",
  "scripts": {
    "post-install-cmd": [
      "npm install",
      "npm run build",
      "mkdir -p public_html",
      "cp -r dist/* public_html/",
      "cp public/.htaccess public_html/"
    ]
  },
  "require": {
    "php": ">=7.4"
  }
}
